<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demotxt.myapp.myapplication">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/skol"
        android:label="@string/app_name"
        android:roundIcon="@drawable/skol"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.FirstScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".activities.MainActivity">

        </activity>
        <activity android:name=".activities.LoginActivity">

        </activity>
        <activity android:name=".activities.SignupActivity">

        </activity>

        <activity android:name=".activities.AnimeActivity">

        </activity>
        <activity android:name=".activities.MakeCult">

        </activity>
        <activity android:name=".activities.GroupAddClass">

        </activity>
    </application>

</manifest>